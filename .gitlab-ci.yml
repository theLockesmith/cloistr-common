default:
  tags:
    - local

stages:
  - test

variables:
  GO_VERSION: "1.25"
  GIT_STRATEGY: none

.go-cache: &go-cache
  cache:
    key: ${CI_COMMIT_REF_SLUG}-go
    paths:
      - .go/pkg/mod/

test:
  stage: test
  tags: [local]
  image: golang:${GO_VERSION}-alpine
  <<: *go-cache
  variables:
    GOPATH: ${CI_PROJECT_DIR}/.go
  before_script:
    - for i in 1 2 3 4 5; do apk add --no-cache git && break || echo "Attempt $i failed, retrying in 5s..." && sleep 5; done
    - |
      for i in 1 2 3 4 5; do
        git clone --depth 20 --branch ${CI_COMMIT_REF_NAME} ${CI_REPOSITORY_URL} /tmp/src && break
        echo "Clone attempt $i failed, retrying in $((i*5))s..."
        sleep $((i*5))
      done
    - cp -r /tmp/src/. .
    - git checkout ${CI_COMMIT_SHA}
  script:
    - go mod download
    - go test -v -race -coverprofile=coverage.out ./...
    - go tool cover -func=coverage.out
  coverage: '/total:\s+\(statements\)\s+(\d+\.\d+)%/'

lint:
  stage: test
  tags: [local]
  image: golangci/golangci-lint:latest
  <<: *go-cache
  before_script:
    - |
      for i in 1 2 3 4 5; do
        git clone --depth 20 --branch ${CI_COMMIT_REF_NAME} ${CI_REPOSITORY_URL} /tmp/src && break
        echo "Clone attempt $i failed, retrying in $((i*5))s..."
        sleep $((i*5))
      done
    - cp -r /tmp/src/. .
    - git checkout ${CI_COMMIT_SHA}
  script:
    - golangci-lint run --timeout 5m ./...
